import React from "react";
import { View, StyleSheet } from "react-native";

import Colors from "../../constants/Colors";

const ArticleViewHeader = (props) => {
  return (
    <View>

    </View>
  );
};

const styles = StyleSheet.create({

});

export default ArticleViewHeader;
